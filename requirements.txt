# Firecrawl App - 生产环境依赖
# 核心框架
flask==2.3.3
flask-cors==4.0.0
requests==2.32.3
urllib3==1.26.18  # 兼容旧版 OpenSSL (1.0.2k)
Werkzeug==2.3.7

# 数据存储
redis==4.6.0

# 网页处理
selenium==4.15.0
beautifulsoup4==4.12.2
webdriver-manager==4.0.1
lxml==4.9.3

# 智能提取
newspaper3k>=0.2.8
lxml_html_clean
nltk==3.8.1
Pillow==10.0.0

# 🔥 增强内容提取库（多种算法对比选择最佳结果）
trafilatura>=1.6.0          # 强大的网页正文提取
readability-lxml>=0.8.1     # Mozilla的文章提取算法（Firefox阅读视图）
htmldate>=1.4.0             # 专业的网页日期提取
justext>=3.0.0              # 基于启发式的正文提取
boilerpy3>=1.0.6            # boilerpipe样板内容移除算法
extruct>=0.16.0             # 结构化数据提取（JSON-LD, Schema.org）
selectolax>=0.3.0           # 超快的CSS选择器解析

# 动态内容提取
playwright==1.40.0

# 数据处理
python-dateutil==2.8.2

# 时区支持
pytz==2023.3
backports.zoneinfo==0.2.1; python_version < '3.9'

# 生产服务器
gunicorn==21.2.0
gevent==23.9.1

# 系统监控
psutil==5.9.5

# 简繁体转换
opencc-python-reimplemented>=0.1.7

# 反爬绕过（模拟浏览器TLS指纹）
curl-cffi>=0.5.0
browserforge>=1.1.0         # 生成更真实的浏览器请求头/指纹

# 大规模定时任务稳定性
tenacity>=8.2.3             # 统一重试、指数退避和随机抖动
requests-cache>=1.1.1       # 缓存robots/sitemap/feed等低风险发现请求

# 在线助手联网搜索
duckduckgo_search>=6.0.0
